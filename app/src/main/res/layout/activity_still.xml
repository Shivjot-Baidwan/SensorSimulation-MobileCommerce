<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_still"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.mobilecommerce.sensorsimulation.StillActivity">
<!--android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" -->

    <TextView
        android:layout_width="match_parent"
        android:layout_height="223dp"
        android:id="@+id/stillScreenTextView"
        android:text="@string/still_screen_message"
        android:textSize="30sp"
        android:textStyle="normal|bold"
        android:textAlignment="center" />


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/musicPlayButton"
        android:text="@string/still_music_play"
        android:onClick="playMusic"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="15dp"
        android:layout_marginBottom="16dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/musicStopButton"
        android:text="@string/still_music_stop"
        android:onClick="stopMusic"
        android:layout_alignBaseline="@+id/musicPlayButton"
        android:layout_alignBottom="@+id/musicPlayButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/musicNextSongButton"
        android:text="@string/still_music_next_song"
        android:onClick="playNextSong"
        android:layout_alignBaseline="@+id/musicStopButton"
        android:layout_alignBottom="@+id/musicStopButton"
        android:layout_toEndOf="@+id/musicStopButton"
        android:layout_marginStart="12dp" />

    <ImageView
        android:layout_width="177dp"
        android:layout_height="248dp"
        android:id="@+id/stillImageView"
        android:src="@drawable/stillimage"
        android:layout_gravity="center|bottom"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@+id/musicPlayButton" />

</RelativeLayout>
